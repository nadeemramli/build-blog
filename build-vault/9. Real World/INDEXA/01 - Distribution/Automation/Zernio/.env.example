# Copy to ".env" (never commit the real key) and load it before running, e.g.:
#   export $(grep -v '^#' .env | xargs) && node accounts.mjs
# Get your key: Zernio dashboard → Settings → API Keys → Create API Key (shown once).
ZERNIO_API_KEY=sk_your_64_hex_key_here
