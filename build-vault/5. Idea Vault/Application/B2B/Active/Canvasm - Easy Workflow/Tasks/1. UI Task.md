---
draft: true
title: "1. UI Task"
date: "2025-08-13"
tags: 
---



Debug state should be able to close. Let's do an icon, that allow user close and open it. Also debug state should only be for dev mode.


Control panel on the left should also be able to collapsible



We can't drag nodes
We can't connect nodes
We can't open nodes sheet.


I love how the app looks when we zoom out 80%. Let's make this zoom out feels "default" for the app.


We had issues in our canvas specifically:  
  
1. Our canvas has two state. One for editing, and one for drawing. For editing it uses the basis of Reactflow, which user can use Nodes, Edge, Evidence, Comments, and comprehensive interaction betweens components such as correlation analysis, etc.  
  
2. In our editing state, we still had issues to add other type of nodes aside from MetricCard node. If I added comment node, all nodes disappear visually (but still exist in Debug State), same goes with other types of Nodes such as Operative, etc.  
  
3. We're still unable to draw using Excalidraw at Draw State. Previously when we tried to use Excalidraw tools directly, it work (using Excalidraw Tool directly), then we hide the tools and wired the tool API through our top toolbar, but it seems we're still unable to use them properly.  
  
4. The canvas should glued together between Edit and Draw, let's create a mechanism where we can "peg" the canvas to each other, ensuring their position X-Y remain the same, constant between these two canvas. The whiteboard or draw become a transparent overlay on top of the reactflow canvas. But to ensure the app work as expected, we need to ensure the overlay always align with the reactflow canvas (their X and Y) should be same.  
  
Let's work on this.