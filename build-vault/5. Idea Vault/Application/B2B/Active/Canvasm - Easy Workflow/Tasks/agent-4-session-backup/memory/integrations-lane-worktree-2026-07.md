---
name: integrations-lane-worktree-2026-07
description: "Integrations agent lane (worktree ../metrimap-integrations): CVS-24/26 shipped, 25 parked, 27/28 evals, DB-hardening CVS-127 started"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9777265e-4271-4c6e-a057-09a8e6f516f9
---

Multi-agent build: I ran the **Integrations** lane in an isolated git worktree at `../metrimap-integrations` (created `--detach` off `origin/main` since `main` is checked out in the primary tree). Setup: symlink `node_modules` + `.env` from the main checkout so type-check/lint/vitest work; per-issue branches; **merge-as-you-go** (see [[commit-and-merge-preference]]); stay off `src/features/canvas/` (owned by another agent). Retarget the same worktree to another lane by swapping the project + issue list.

**Lane outcomes (2026-07-04, team CVS/Canvasm):**
- **CVS-24** (Initiatives ↔ metric tree) — DONE→In Review, PR #17 merged. Created live Initiative "Grow Profit — value-tree objective" anchored to the `Profit` `metric_cards` node; convention doc `docs/features/linear-initiatives.md`. Manual test CVS-106.
- **CVS-26** (Userback → Customer Requests) — DONE→In Review, PR #19 merged. Server-side edge fn `userback-customer-requests` + migration `userback_feedback` (RLS on/no policy) + `userback_sync_config()` Vault RPC + `npm run sync:userback` + `docs/features/userback-customer-requests.md`. Mirrors the sync-error-reports pattern. Manual test CVS-110. Not deployed (owner step).
- **CVS-25** (Linear Releases) — **PARKED, billing-blocked**: Releases is Business/Enterprise-only; owner confirmed workspace is on a lower plan. Deprioritized to Low. No build.
- **CVS-27 (Sentry) / CVS-28 (Snyk)** — written adopt-vs-build recommendations only (no code), In Review. Both need a **human setup step first** (Sentry account+DSN+Linear integration; Snyk GitHub App OR the free Dependabot+CodeQL path) — checklists posted on each issue; I build once done.
- **CVS-127** (DB security hardening) — new System Health issue from the 96 Supabase security advisors. Phase 1 PR #24 **open, NOT self-merged** (prod grants). Manual test CVS-128. Phase-2 GraphQL disable = owner dashboard toggle; anon-helper revokes are accepted-by-design (doc PR #43 merged). CVS-28 free path (Dependabot+CodeQL) shipped PR #26.
- **MCP lane** (project "Programmatic building — MCP + API"): design spike CVS-97 is settled in the vault (`PRD/4. Product/3. MCP and Programmatic Building`). **CVS-98 SHIPPED** (PR #49 merged): `src/shared/lib/api/` — `createMetrimapApi(client, userId)`, RLS-scoped Zod-validated facade over metric_cards+relationships+projects+tracked-values services (14 tests). Manual test CVS-184. **CVS-100 SHIPPED** (PR #52 merged): `src/shared/lib/api/mcp/` — transport-agnostic tool registry + dispatch + structured McpToolError + AuthContextResolver seam (CVS-99 impls it); 10 tests; manual test CVS-186. **CVS-101 SHIPPED** (PR #54): create_driver_node + subCategory support + `layout_tree`/`tree.layout` (reuses `src/shared/utils/autoLayout.ts` Dagre, persists positions); manual test CVS-188. **CVS-102 BUILT** (PR #56 — NOT self-merged, adds RLS policies + import_batches/import_rows tables + hand-patched types.ts; owner reviews + db push): `src/shared/lib/api/ingest.ts` stage(series/CSV, TTL)→materialize onto card.data (+ metric_values sync); MCP tools stage_series/upload_csv/materialize; manual test CVS-189. MCP lane: CVS-98/100/101 merged, CVS-102 open-for-review. **CVS-104 BUILT** (PR #62 — NOT self-merged, adds mcp_audit_log RLS table): `src/shared/lib/api/mcp/{guards,audit}.ts` — RateLimiter + payload caps + audit sink → dispatchTool(name,args,ctx,guards); manual test CVS-196. **MCP lane build DONE: CVS-98/100/101 merged; CVS-102/104 open-for-review.** **CVS-99 BUILT** (PR #69 — NOT self-merged, SECURITY DEFINER RPC): API-key AuthContextResolver — `src/shared/lib/api/mcp/auth/{jwt,apiKeyResolver}.ts` mints a short-lived Supabase JWT (sub=Clerk id via requesting_user_id(), o.id=org) with the SUPABASE_JWT_SECRET; hashApiKey matches apiKeys.ts (mk_live_ + sha256); mcp_resolve_api_key RPC. OAuth Connect = documented follow-up. Manual test CVS-203.

**🎉 MCP LANE BUILD COMPLETE: CVS-97(vault)→98→99→100→101→102→104.** Only CVS-103 (connect UX/docs) remains as build work (buildable now — settings "Connect your agent" page). To go LIVE the owner must: review+merge+db-push the 4 open DB PRs, pick hosting (Vercel fn vs Node), deploy the transport adapter (add @modelcontextprotocol/sdk there), set SUPABASE_JWT_SECRET server env, wire createApiKeyAuthResolver + CVS-104 guards.

**Open PRs awaiting owner (all prod-DB, not self-merged): #24 (DB grants), #56 (ingest RLS), #62 (audit RLS), #69 (auth RPC).** Merged this lane: #17,19,26,43,49,52,54. **Open PRs awaiting owner review (all prod-DB, not self-merged): #24 (DB grants), #56 (ingest RLS), #62 (audit RLS).** types.ts hand-patches: #56 adds import_batches/import_rows at feed_bookmarks region; #62 adds mcp_audit_log at metric_card_tags region (different regions → no conflict).

**Workflow change 2026-07-04 (AGENTS.md updated):** manual-test sub-issues now go in the **Manual Test** project assigned to the **owner** `m.nadeemramli@gmail.com` (NOT the old Checkpoint user) so they land in his Todo. Owner does the manual testing now.

**Reusable technical facts:**
- The app is **pure PostgREST** — the pg_graphql endpoint is **unused** (no `graphql` in `src`). So the 56 `pg_graphql_*_exposed` advisors are best cleared by **disabling the GraphQL endpoint**, NOT by revoking table SELECT (which would break PostgREST+RLS). [Phase 2 of CVS-127.]
- Advisor triage: `error_report_groups` + `userback_feedback` RLS-no-policy and `error_reports` always-true INSERT are **intentional** (service-role / anon-crash-report) — do not "fix".
- Checkpoint user id `a9621313-b3af-4eef-bcff-8e85d50031a6`; `manual-test` label `0a9e9434-75da-4335-8c88-371242f328b4`; Manual Test project `9e8f3c9d-...`. Linear MCP authors as **Nadeem Ramli** (connected account), not a bot.
