---
name: metrimap-handoff-2026-06
description: Metrimap current state — operators/simulation DONE & verified; handoff for a fresh chat
metadata: 
  node_type: memory
  type: project
  originSessionId: 026e73b4-2fda-468e-9def-65bb4a109f72
---

Metrimap (visual metric-tree app): React 19 / Vite 7 / TS, cloud Supabase ref `iqrclwolhookzzmiedun`, Clerk `pk_live`, deployed on Vercel at use.canvasm.app. Demo Clerk user id: `user_30uQARGezqSApAChPSGSwFyUy85`. Workflow: branch `feature-organization` → `git merge --ff-only` into `main` → push (commit/push only when the user asks; push deploys). All DB work goes through the Supabase MCP (the pooler DB password does not auth); the seeder inserts via the service-role key from `.env`.

**Done this stretch:**
- **Showcase**: 3 public example trees (SaaS / E-commerce / Retail), tags `example`+`template`, owned by the demo user. Homepage `ShowcaseSection` is read-only and examples are excluded from the deletable list. Seeder `scripts/seed/seed-example-trees.mjs` is re-runnable (deletes by name, inserts via service role), uses `groupClusteredLayout` (each group → its own clean non-overlapping grid frame), seeds 7-month `metric_cards.data` series, and 4 groups/tree.
- **Grouping fully working**: create / ungroup / delete wired in `useCanvasEvents` + `CanvasPage` (were dead no-ops); group→dashboard via `group.nodeIds`; Figma-style frame in `group-node.tsx` (header = drag handle `.group-drag-handle`, paints behind cards via render-order + `zIndex 0`, removed `extent:'parent'` + the auto-resize-on-select bug); **group-aware auto-layout** (frames recompute to wrap their cards after Dagre).
- Operator **Phase A** (tool drives real operator nodes, persisted via `useCanvasNodesStore`). Lint made useful (`eslint.config.js`: 1070→53 errors). Autosave RLS fix (pass the authenticated client).

**Done 2026-06-27 (now on `main`, pushed):**
- **Operator Phase B+C DONE.** B: pipeline edges persist in `projects.settings.dataFlowEdges` (jsonb, hydrate on load / write on connect / `onEdgesDelete` removes). C: `src/features/canvas/utils/runSimulation.ts` — topo-order operators, **sum** upstream as `x`, formula(mathjs worker)/boolean-gate/datePicker-passthrough, **append** a new MetricValue point to downstream cards via `persistNodeUpdate`. Decisions: append / sum / cards+source-nodes as input. `MetricCard.tsx` now shows latest-3 (was oldest-3). Demo seeded in SaaS example (`ad1f3617…`): operator `Forecast ×1.1` + card `Projected MRR (Forecast)` + 2 dataFlowEdges (via Supabase MCP, re-runnable).
- **Grouping redesigned: frame → focus/filter.** On-canvas group rectangle DROPPED (group data kept). New `GroupsPanel.tsx` (top-right toggle): focus / rename / recolor / add-remove selected / create-from-selection / delete. **Focus mode** dims non-members + boundary edges (`.rf-node-dimmed`/`.rf-edge-dimmed` in `styles/index.css`) and fit-zooms; Esc exits. Edges dim via `className` (DynamicEdge hardcodes path opacity). `group-node.tsx` + `convertToGroupNode` now unused; `SelectionPanel` ungroup button is inert.
- **Auto-layout reads as a tree + no overlapping edges (ELK).** `convertToEdge` anchors relationship edges to directional handles (TB: parent `bottom-source`→child `top-target`) + weight-colored `markerEnd` arrowheads. Auto-layout now uses **ELK** (`elkjs`, lazy-imported, own `vendor-elk` vite chunk) `layered`+`ORTHOGONAL` routing — `src/shared/utils/elkLayout.ts` returns positioned nodes + per-edge routed bend-points; `DynamicEdge` draws the rounded orthogonal polyline from those (`data.routedPoints`), else smoothstep. `routedEdgePoints` state in CanvasPage; dragging a node drops its incident edges' routes. Dagre (`applyAutoLayout`) kept for tests/other callers but no longer used by the canvas. All relationship edges are smoothstep/orthogonal (bezier dropped).

**Operator TOTAL REVAMP — DONE 2026-06-27 (on main, pushed).** Plan: `the Obsidian product vault`. New model: metric card = placeholder; operative node combines NAMED inputs (a,b,c by connection order, renameable; `x`=sum alias kept for back-compat) → chains into next card. New op types: formula/aggregate/gate/toggle/statistical (datePicker dropped). Files: `types/operator.ts`, `utils/operatorMigration.ts` (lazy read-time migration boolean→toggle/datePicker→passthrough), `utils/operatorInputs.ts`, `utils/computePipeline.ts` (PURE compute, no writes), `runSimulation.ts`→`runPipeline` (persist + before→after changes; `runSimulation` alias kept), `stores/useOperatorPreviewStore.ts` + `hooks/useLivePreview.ts` (debounced loop-safe live preview), `operator-node.tsx` (presentational: badge+input chips+live output), `ControlPanel.tsx` (tabbed: Operator/Pipeline/Global). CanvasPage: edge lifecycle binds/unbinds operator inputs; `onNodeClick` selects operator for the panel. Card Settings sheet also fixed earlier (was a stub `layout/CanvasModals`). NOTE: user has parallel uncommitted work (edges DynamicEdge/Erasable/EnhancedEdgeButton, RelationshipSheet+constants, several new the Obsidian product vault*.md`, `canvas/constants/`, `InfoHint.tsx`) — do NOT `git add -A`; stage specific files.

**Verified 2026-06-28:** operator pipeline confirmed end-to-end. Compute lives in `utils/computePipeline.ts` (pure) + `runSimulation.ts` (`runPipeline` persists). `onSimulate` wired via `handleRunPipeline` (CanvasPage:1393, real `isSimulating`). Edges persist/hydrate from `projects.settings.dataFlowEdges`. Type-check green. DB trace of SaaS demo (`ad1f3617…`): MRR 774210 → operator `x*1.1` → output card 851631. NOTE: that seeded operator still carries legacy `operationType:"datePicker"` + `formula:"x * 1.1"` — works only because `normalizeOperatorData` maps datePicker→formula and KEEPS the existing formula; output card `Projected MRR (Forecast)` has `data=null` until first Run. Cosmetic cleanup available: re-stamp it as `operationType:"formula"`.

**Pending:** user evals operator revamp on live deploy. Optional: re-stamp the demo operator to `formula` type (drop legacy datePicker reliance); source-node numeric value editing UI; remove inert SelectionPanel ungroup button; mark old `operator-control-feature.md` superseded.

**Gotchas:** `metric_cards` has **no `parent_id` column** → `card.parentId` is client-only and lost on reload; groups persist membership in `node_ids`, so always filter group members by `nodeIds`, not `parentId`. Open in `BACKLOG.md`: 29 latent Rules-of-Hooks violations (P2) and service-role key rotation (P0). See [[supabase-rebuild-2026]].
