---
name: object-model-locked
description: "The 4 object-model/catalog forks locked 2026-06-28 (Workspace→Space→Canvas, Clerk-org workspace, promotion-based Tracked-Metric catalog, two-tier values)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4acf6cca-ea6d-4342-91f6-be01eb6c413d
---

Object model LOCKED with owner 2026-06-28 (full detail + diagram in `the Obsidian product vault`):

1. **Nesting:** Workspace → Space → Canvas → Node. Today `project` IS the canvas (1:1, flat); rename project→Canvas, add Space (folder) + Workspace tiers. **B.3a SHIPPED 2026-06-29:** `spaces` table (owner-RLS) + `projects.space_id` on prod; `services/spaces.ts`; store loads spaces + create/rename/delete/moveProjectToSpace; homepage Spaces filter row (All/Uncategorized/each/+New) + "Move to Space" on cards/table; `useProjectFiltering2` spaceFilter. **B.3b/B.4 IN PROGRESS:** Clerk Orgs ENABLED 2026-06-29 (auto-create-first-org ON + naming "{{first_name}}'s org" fallback "My Workspace" + slugs + user-created limit 1, creator=Admin). `<OrganizationSwitcher>` SHIPPED to homepage header (commit 2f40498, B.3b stage 1, cosmetic until scoping). **Org-scoping folded into B.4** (owner chose: test with real teammate, not flip blind). **Design prepared + SAFE (additive)** in `the Obsidian product vault`: org claim = `request.jwt.claims->'o'->>'id'`; add `requesting_org_id()` + `workspace_id` cols on 5 direct tables (projects/spaces/tracked_metrics/metric_values/source_connections); the 8 indirect tables route through 2 helper fns (`can_view_project`,`has_project_access`) → update those 2 ONCE; ADD `OR workspace_id=requesting_org_id()` (additive → owner never locked out); backfill workspace_id=org_id. Keep users/notifications/changelog per-user. **APPLIED to prod 2026-06-29** (commit ad8e8f9, `supabase/migrations/20260629150000_workspace_org_scoping.sql`): `requesting_org_id()` reads `o.id`; workspace_id on 5 tables w/ DEFAULT requesting_org_id() (new rows auto-inherit active org, NO app change); helpers + policies additive; **backfilled 6 projects → org_3FoSILwCPoJqixKBkoMyC8P8OQ0** (owner=user_30uQARGezqSApAChPSGSwFyUy85 / m.nadeemramli@gmail.com). Security advisor clean. **PENDING TEST:** (1) owner regression (sees 6 canvases), (2) claim-flow check — create a canvas, confirm its workspace_id auto-set to org (proves Clerk `o.id` claim reaches Supabase JWT), (3) invite teammate to org → sees shared workspace. CLAIM CONFIRMED FLOWING (new canvas auto-got workspace_id=org via DEFAULT). **ORGS-ONLY model chosen 2026-06-29:** strict workspace separation applied (commit 466406e, migration 20260629160000) — reads/writes scope to ACTIVE org's workspace_id + null-workspace creator fallback (no orphan); owner-clause leak removed. App (commit 8fc7bba): OrganizationSwitcher `hidePersonal` + ProtectedRoute auto-activates first org if none active (so org-scoped data never reads empty). User's workspace = "Teroka Digital" (org_3FoS…, 7 canvases). **WORKING END-TO-END 2026-06-29** (verified by owner: invited teammate sees shared canvases, realtime sync works). KEY LESSON: org-scoping needs BOTH RLS *and* the FETCH query — `getUserProjects` was owner-scoped (`.eq('created_by', userId)` + collaborators) so teammates saw nothing even though RLS allowed it; fixed to a single RLS-scoped query (commit a6d45b5). Also: real-time project-list sync (`useProjectsRealtime`, postgres_changes on projects filtered by workspace_id, commit 45f6bac); homepage refetches on org switch + force flag (edd2ce0). Workspace = "Teroka Digital" (org_3FoS…). Spaces row is a chip-row now; sidebar is a polish upgrade. NOTE: project count dropped 8→4 during testing (Test test + 3 example trees gone) — owner believed to have deleted during testing; unconfirmed if a bug.
2. **Workspace = Clerk Organization** — members/invites/roles inherited from Clerk; catalog + value store are workspace-scoped.
3. **Promotion-based Metric Catalog:** a **Tracked Metric** = (Source Node + Metric Card) operationalized with real data. Only these get catalogued. 3 states: exploratory/proxy (no source, per-canvas) → tracked **candidate** (auto, on binding, "Candidates" tab) → **catalogued** (explicit "Catalog this metric" button, workspace-scoped, reusable). Referencing a catalogued metric on another canvas shares **definition AND values** (one number). NO force-migrate of the ~142 cards — catalog fills by promotion. Birth moment = Source Node "Save & feed downstream" (already shipped). Multi-select wired nodes → **Pipeline Template** (structure + source config, not data).
4. **Two-tier values:** exploratory cards keep inline `data` jsonb; Tracked Metrics get a snapshot store `{period,value,snapshot_at,source,version}` keyed by tracked_metric_id → freshness/stale-state/time-travel/scenarios. Supplement (write-through), not replace.

Removed/broken connection → node keeps last snapshot + visible **stale/orphaned-binding** state, never silent zero/delete. Two backlogs: (A) Homepage & Account (ships now), (B) Platform Epics (the 4 migrations). Related: [[data-source-phase1]].
