---
name: multi-agent-build-workflow
description: "Repo is in a multi-agent build phase — work from Linear team CVS, branch-per-issue + PR (NOT direct-to-main), docs policy"
metadata: 
  node_type: memory
  type: project
  originSessionId: 013bc508-1cef-453e-8fd4-8a980115bb62
---

As of 2026-07-04 the repo runs a **multi-agent build** driven by Linear (team **CVS**, Initiatives → Projects → Issues). The operating protocol was installed into `AGENTS.md` via **CVS-45** (bootstrap, PR #10).

**This SUPERSEDES [[commit-and-merge-preference]] direct-to-main during the build.** Now: branch per issue using the Linear `gitBranchName`, keep `CVS-XX` in branch/PR, PR → move issue to **In Review** (never straight to Done) → create a **manual-test sub-issue**. **⚠️ Manual-test routing changed via CVS-125 (2026-07-04, merged PR #45): Checkpoint account DROPPED.** A manual-test sub-issue is now: child of the feature issue, title `Manual test: <feature>`, **in the Manual Test project** (`projectId 9e8f3c9d-cf81-4238-a9e1-35d6c9324355`), **assigned to the owner Nadeem** (`assigneeId 3f0b8049-7811-47da-93f8-5d93770d4348`, m.nadeemramli@gmail.com), label `manual-test`. NOT checkpoint (old id a9621313… is dead). A feature is Done only after the owner passes the manual test + merged. Read each issue's `## Deep analysis` + honor `## Decision (locked)` comments.

**Docs policy** (in AGENTS.md): product knowledge (PRD, feature narratives, methodology, learning) → owner's Obsidian product vault; repo `docs/` = infra/structural only (ADR, database/RLS, auth, environment, state-management, architecture, infra features like system-health-intake/linear-setup/metrics-api). No changelogs/point-in-time docs; manual tests are Linear sub-issues, not a repo file.

**CVS-45 status: DONE + merged (2026-07-04).** PR #10 (CVS-45) merged to main = AGENTS.md operating protocol + docs policy, fixed root README (was the wrong Supabase-CLI readme), docs prune, product docs migrated to the Obsidian vault (`5. Idea Vault/Application/B2B/Active/Canvasm - Easy Workflow/Archive or References/Repo Docs Migrated 2026-07-04`), VitePress retired, dev-tooling overhaul (husky + lint-staged). PR #11 (CVS-71) merged = further repo-doc prune. Net: `docs/` is infra/structural Markdown only; `docs/backlog/*`, `BACKLOG.md`, `docs/prd`, `docs/reference`, VitePress all gone. Manual-test sub-issue = CVS-47.

**Backlog reconciliation (owner rule: file the issue before losing a backlog doc):** CVS-71 deleted the backlog docs; I reconciled them against Linear and filed the genuine unfiled gaps as **CVS-73…CVS-83** (Triage): assigned-notif producer, server-side user provisioning (P0-7 real bug), Cmd+K palette, orphan auto-group, realtime one-step-behind (rel CVS-40), notif bell badge, typed owner, RCA/drift (deprioritized), notif prefs backend, auth-client+rules-of-hooks debt, global search. Public-embed gap was already CVS-62/63. Full original detail recoverable from git before commit `a36e24c`.

**⚠️ COLLISION LESSON (2026-07-04):** two agents ran in the SAME working dir (`…/metric-mapping`) — one on `cvs-45-bootstrap-agents-docs`, another on `chore/husky-hook-split` — and corrupted each other (branch flipped mid-command, staged files wiped, files edited under me). `git worktree list` showed NO worktrees. **RULE: never run two agents in the same working tree; each MUST get its own `git worktree` (as the runbook says).** It resolved only because both branches got merged to main.

**Progress (2026-07-04, my lane = canvas + foundation, MERGED to main):** CVS-74 (Clerk-webhook user provisioning), CVS-73 (assigned-notif producer via `notify_card_assigned` RPC), CVS-68 (pane-click/Esc deselect), CVS-66 (removed duplicate BulkOperationsToolbar — it's on the orphan store; SelectionPanel is the real one), CVS-61 (per-node toolbar delete was a stub → wired to persistNodeDelete), CVS-33 (comment-author codenames). **Shared util `src/shared/utils/codename.ts`** (`userCodename`/`codenameInitials`, deterministic Adjective-Noun) — the **Evidence lane (CVS-34/35/36) must REUSE it, not reimplement**. CVS-33 kept real names in the People list + @mention picker on purpose (usability); mentions-to-codename is a flagged follow-up. **CVS-67 (node-type-aware toolbar) DEFERRED** — it's a big 2-toolbar-system refactor (EnhancedNodeToolbar + MetricCard's own 9-icon toolbar + new action types); owner confirmed the per-type manifest, do it as a dedicated pass. Cadence: **merge PRs as you go** (owner: squash-merge my own PRs, don't pile up). Parallel agents 2 (Integrations: CVS-24/26 merged) + 3 (Home & Settings) run in their own worktrees — working cleanly, no collisions.

**Toolbar overhaul DONE (2026-07-04, merged):** CVS-30 (off-screen wayfinding arrow `OffscreenNodeIndicator` + Shift+1 recenter), CVS-22 (Strategy crash — `useProjectMembers` called `getClientForEnvironment()` synchronously → threw before auth-client ready; now awaits `whenAuthenticatedClientReady()`), CVS-31 (consolidated the scattered top-right Groups/Tools/Export cluster into `TopCanvasToolbar` — one bar), CVS-32 (moved that bar top-center→top-right, Groups fly-out offset below it), CVS-67 (node-type-aware per-node toolbar via new **single-source-of-truth manifest `src/features/canvas/components/nodes/shared/nodeToolbarManifest.ts`** — `EnhancedNodeToolbar` derives show* from it by nodeType; settings now hidden on metric/value/action/hypothesis). **CVS-67 follow-ups (not done):** wire richer actions (comment/chart/data/connect/resolve) + migrate `MetricCard.tsx`'s own rich 9-icon toolbar + source/chart/comment/whiteboard node toolbars to read the manifest (map already declares their sets); the 4 nodes' view/edit/settings/copy handlers are still stubs (only delete wired, CVS-61); remove `EnhancedNodeToolbar` `@ts-nocheck` in the type-debt sweep (CVS-64/72). Linear API flaked once (upstream timeout) mid-run — use a retry/backoff + dup-guard wrapper when scripting it.

**Parallel build (owner-driven, runbook):** 4 lanes via git worktrees + 4 terminals — A Integrations, B Home (CVS-29), C Evidence (CVS-35/36/34), D Canvas-engine SERIAL (owns CanvasPage.tsx+canvasStore.ts). I can't spawn interactive terminals — that part is the owner's to run; I can do single-issue tasks here. Linear MCP not connected in my sessions — I use the Linear GraphQL API with the key in .env instead. Related: [[system-health-linear-intake]].
